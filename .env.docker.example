# Скопируйте в .env рядом с docker-compose.yml при необходимости:
# cp .env.docker.example .env

# Порт на хосте → внутри контейнера приложение слушает 3000
PORT=1337

# Рекомендуется для production (32 байта в base64):
# SSH_TERMINAL_MASTER_KEY=
